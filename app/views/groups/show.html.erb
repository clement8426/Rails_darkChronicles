<h1><%= @group.name %></h1>

<h2>Utilisateurs :</h2>
<ul>
  <% @group.users.each do |user| %>
    <li><%= user.name %></li>
  <% end %>
</ul>

<h2>Feuille :</h2>
<%= render partial: 'sheets/sheet', locals: { sheet: @group.sheet } %>
